rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TESTING RULES - Allow all authenticated users
    // ⚠️ JANGAN DIPAKAI DI PRODUCTION!
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
